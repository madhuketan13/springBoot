spring:
  datasource:
      username: root
      password: root
      url: jdbc:mysql://localhost:3306/dnb2
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 9000